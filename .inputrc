# NOTE: The `~/.inputrc` file sets the options for GNU Readline. GNU Readline is used in bash
# but also in a variety of other prompts.

# Do not ring the bell ever
set bell-style none

# Case insensitive completion
set completion-ignore-case on
